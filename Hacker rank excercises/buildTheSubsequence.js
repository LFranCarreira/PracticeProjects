// Build The Subsequences should return all the subsequences of a string in an array full of strings
function buildSubsequences(s) {
  // Return case
  if (!s.length) {
    return;
  }
}

buildSubsequences('xyz');
// The output should be ["x", "xy", "xyz", "xz", "y", "yz", "z"]
